# Cat Watcher Configuration
# Copy this to .env and update with your values
#
# NOTE: Most settings are now configurable via the web UI (Settings page)
# and stored in the SQLite database. Only secrets and paths need to be
# set here or via environment variables.

# ===========================================
# Secrets (credentials - keep these in .env or K8s secrets)
# ===========================================
# RTSP credentials for camera streams
# CAT_WATCHER__FRIGATE__RTSP_USERNAME=your_camera_username
# CAT_WATCHER__FRIGATE__RTSP_PASSWORD=your_camera_password

# MQTT credentials (if authentication is enabled)
# CAT_WATCHER__MQTT__USERNAME=your_mqtt_username
# CAT_WATCHER__MQTT__PASSWORD=your_mqtt_password

# ===========================================
# Paths (usually don't need to change)
# ===========================================
# DATA_DIR=./data
# MODELS_DIR=./models

# ===========================================
# Logging
# ===========================================
LOG_LEVEL=INFO

# ===========================================
# Web UI Configurable Settings (set these via UI instead)
# ===========================================
# The following are now configured via the Settings page in the web UI:
#   - Frigate URL and cameras
#   - Detection thresholds (cat confidence, behavior confidence)
#   - Frame rate
#   - Event durations
#   - MQTT broker host and port
#   - Save frames option
